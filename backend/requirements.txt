fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
httpx>=0.27.0
python-dotenv>=1.0.1
apscheduler>=3.10.4
scikit-learn>=1.4.2
numpy>=1.26.4
xgboost>=2.0.3
python-multipart>=0.0.9
websockets>=12.0
pydantic>=2.7.1
bcrypt>=4.1.3
python-jose[cryptography]>=3.3.0

# PostgreSQL async driver (used when WORKER/ADMIN/ANALYTICS_DB_URL = postgresql+asyncpg://...)
asyncpg>=0.29.0

# Redis pub/sub for cross-process WebSocket broadcast (used when REDIS_URL is set)
redis>=5.0.0

# Razorpay: using httpx (already listed above) for API calls — no extra SDK needed
